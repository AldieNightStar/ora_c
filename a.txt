use out
use math
use swaps
use debug
use logic

func main
	20 loop:
		dup 0 eq ?end
		dec
		dup printn
		inner
		!loop
	end:
		"DONE!" prints

func inner
	5 loop:
		dup 0 eq ?end
		dec
		dup printn
		!loop
	end:
		drop